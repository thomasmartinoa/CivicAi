# ─── Database ──────────────────────────────────────────────
# SQLite (default for local dev — no setup needed)
DATABASE_URL=sqlite:///./civicai.db
# PostgreSQL (production/Docker): DATABASE_URL=postgresql://civicai:civicai@localhost:5432/civicai

# ─── Auth ──────────────────────────────────────────────────
SECRET_KEY=change-me-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# ─── LLM Provider ──────────────────────────────────────────
# Choose one: "gemini" | "anthropic" | "openai"
LLM_PROVIDER=gemini

# Google Gemini — get key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Anthropic Claude — get key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# OpenAI (also used for Whisper voice transcription) — https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ─── Email / SMTP ──────────────────────────────────────────
# Local dev: docker run -p 1025:1025 -p 8025:8025 mailhog/mailhog
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=

# ─── Storage / OTP ─────────────────────────────────────────
UPLOAD_DIR=./uploads
OTP_EXPIRE_MINUTES=10
